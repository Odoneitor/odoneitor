<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="softonic.list">
      <field name="name">softonic list</field>
      <field name="model">softonic.softonic</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="softonic.action_window">
      <field name="name">softonic window</field>
      <field name="res_model">softonic.softonic</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="softonic.action_server">
      <field name="name">softonic server</field>
      <field name="model_id" ref="model_softonic_softonic"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="softonic" id="softonic.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="softonic.menu_1" parent="softonic.menu_root"/>
    <menuitem name="Menu 2" id="softonic.menu_2" parent="softonic.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="softonic.menu_1_list" parent="softonic.menu_1"
              action="softonic.action_window"/>
    <menuitem name="Server to list" id="softonic" parent="softonic.menu_2"
              action="softonic.action_server"/>
    -->
  </data>
</odoo>